# Tuple is like a list but like the list tuple data can not be changed
# In the list we can use append , remove ,del etc to change the array data but in tuple we cannot use this 
my_tuple=('Apple','Banana',1,2,3,'@','Apple')
print(my_tuple[3])
print(my_tuple[5])
print(my_tuple)
print(len(my_tuple))
print(my_tuple.count('Apple'))
# In both list and tuple we can use double elements in the list 